{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch MatchstickGame",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/MatchstickGame.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "CMake Build",
            "miDebuggerPath": "C:/mingw64/bin/gdb.exe"  // Убедитесь, что путь к gdb.exe корректный
        }
    ]
}
